$('#projects').append("<%= j render(@projects) %>");

<% if @projects.next_page %>
  $('.pagination').replaceWith("<%= j  will_paginate(@projects, renderer: 'BootstrapPaginationHelper::LinkRenderer') %>");
<% else %>
  $('.pagination').remove();
<% end %>